export { supabase } from './supabase/client';
// Server-only exports are not included here to prevent client-side imports
// For server components, import directly: import { createSupabaseServerClient } from '@shared/api/supabase/server';

